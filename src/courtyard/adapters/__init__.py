"""Adapters: what connects an agent to the hub (design §7). One package per agent type:
Claude Code, pi, and the dummy, the scripted or human-driven reference implementation of
the adapter contract."""
